
# Keep only the Terraform version here.
# The `required_providers` is already in main.tf.


terraform {
  required_version = ">= 1.2.0"
  # required_providers moved to main.tf to avoid duplication
}
